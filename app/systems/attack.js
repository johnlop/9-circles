// ECS - System - Attack
// --------------------------------------
ECS.systems.attack = function renderSystem(entities) {
  var entity, dir;

  for (var id in entities) {
    entity = entities[id];

    if (entity.components.target) {
    }
  }
};
